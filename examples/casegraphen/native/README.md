# Native CaseGraphen Reference Case

This directory retains native `CaseSpace` and `MorphismLog` report fixtures used
by HigherGraphen tests after the CaseGraphen extraction.

Current native schemas, runnable examples, CLI tests, and operator documentation
live in [`CAPHTECH/casegraphen`](https://github.com/CAPHTECH/casegraphen).
